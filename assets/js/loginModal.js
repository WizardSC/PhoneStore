const loginBtn = $('#login__btn');
const loginUsernameField = $('#login__field-username')
const loginPasswordField = $('#login__field-password')
const alertUsernameField = $('.alert-username')
loginBtn.addEventListener('click', () => {
    // if(User.isExistUsername(loginUsernameField.value)){
    //     console.log("User already exists")
    // } else {
    //     // alertUsernameField.innerText = 'Tên đăng nhập không tồn tại trên hệ thống'
    // }
})
